import axios from "axios";
export default {

    handleGetGoods({commit}){
		// axios({
		// 	methods:"get",
		// 	url:"/mp/order/myorder",
		// }).then((data)=>{
		// 	console.log(data)
			// data.data.filter((item)=>{
			// 	item.flag=false;
			// })
			// commit("handleGetGoods",data.data)
		// });
    }
}
